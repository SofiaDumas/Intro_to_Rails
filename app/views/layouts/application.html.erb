<!DOCTYPE html>
<html>
  <head>
    <%# Sets the page title dynamically if a title is provided, otherwise defaults to "Intro To Rails" %>
    <title><%= content_for(:title) || "Intro To Rails" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%# Enables web app mode on Apple and Android devices %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Placeholder for injecting additional head content from specific views if needed %>
    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%# Loads JavaScript modules managed by importmap-rails %>
    <%= javascript_importmap_tags "application" %>
  </head>

  <body>
    <header>
      <%# Main heading for the site %>
      <h1>The Books List</h1>
      <%# Subheading to invite user engagement %>
      <h2 style="color: green"> Which adventure are you looking to dive into next?</h2>
      <%# Navigation menu to different sections of the site %>
      <nav class="nav-links">
        <ul class="nav-row">
          <li><%= link_to "Home", root_path%></li>
          <li><%= link_to "Authors", authors_path%></li>
          <li><%= link_to "Genres", genres_path%></li>
          <li><%= link_to "About", about_path%></li>
        </ul>
      </nav>
    </header>
    <%# Renders the view-specific content inside the layout %>
    <%= yield %>
  </body>
</html>
